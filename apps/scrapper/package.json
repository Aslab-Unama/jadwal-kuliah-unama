{
  "name": "@jadwal/scrapper",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun run src/index.ts --test",
    "sync": "bun run src/index.ts --sync",
    "sync:fresh": "bun run src/index.ts --sync --clean"
  },
  "dependencies": {
    "@jadwal/db": "workspace:*",
    "cheerio": "^1.2.0",
    "dotenv": "^18.0.4"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
